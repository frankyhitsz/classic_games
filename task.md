# 二次审查修复记录

## 当前状态

- [x] 完整阅读二次审查任务书；
- [x] 确认本轮不执行 GitHub 提交或推送；
- [x] 逐项复现 CG2-F01 至 F13；
- [x] 完成第一批可靠性修复；
- [x] 完成第一轮独立复查；
- [x] 完成第二轮独立复查；
- [x] 汇总最终结论和验证证据。

## 初步判断

- F05、F06、F07、F08、F09、F11、F13 从当前代码路径看成立；
- F01–F04 指向同一个异步保存生命周期缺口，需要联合验证和修复；
- F10 的并发竞争成立，但“所有端点必须完全独立退避”是否必要仍需按本机服务语义判断；
- F12 指出了字段命名不准确，但完整 attempts 模型属于后续数据架构，不应在本轮直接重写；
- 默认进程内仓储、pytest/CI、音频、打包和内容扩展属于后续路线，不作为当前 Bug。

## 验证记录

### 初始实现与验证

- 可执行复现了未消费 Future、榜单竞态、推箱子提前确认、2048 畸形 ACK、
  Tetris 同义键释放和跨方块补步；
- 在临时数据库复现了 2048 高分被低分覆盖和数据库故障返回 HTML 500；
- 修复端口传播和直接导入测试的数据库路径；
- 建立保存中/成功/失败状态、有限重试、S 键重试和 ACK 后榜单刷新；
- 加入 BackendClient drain/close、读写退避隔离、严格 ACK 解析；
- 修复 Tetris 物理键记账、方块 generation、dt 上限和显式 board 参数；
- 服务端更新改为分数单调不下降，SQLite 错误统一返回 JSON 503；
- `plays` 改名为准确的 `records`，完整 attempts 模型保留为后续架构；
- 103 项隔离检查通过；默认 `data/scores.db` 哈希和修改时间未变化。

### 第一轮独立复查发现

- 已用请求开始时间与最近失败时间阻止旧成功清除新退避；
- BackendClient 改为幂等关闭，启动器注册退出兜底，独立游戏只关闭自有客户端；
- 共享客户端保留最终失败的保存负载；返回菜单后会显示未保存数量并支持 S 键重试；
- 自动重试复用同一个请求号，服务端记录已处理请求，响应丢失后重发不会重复记分；
- 第一轮针对性复现全部通过，2048 畸形 ACK 安全降级，服务端异常稳定返回 JSON。

### 第二轮独立复查发现

- 五个游戏累计执行 18,000 次随机输入与时间步进，玩法状态不变量保持成立；
- 俄罗斯方块同义键、暂停恢复、长帧和新方块代际边界未再出现补步；
- 40 轮 BackendClient 创建、异步请求和关闭后，线程数回到基线；
- 120 次并发 SQLite 成绩写入完成，未出现锁泄漏或非 JSON 错误；
- dummy 视频驱动下各游戏 80 帧渲染的 p95 为 2.25–4.43 ms，当前环境没有支持静态 Surface 缓存重构的性能证据；
- 发现低分里程碑失败后、高分最终成绩成功仍可能遗留旧失败提示，已用替换分数确认关系清理被高分覆盖的失败项；
- 最终复查发现自动重试缺少幂等保障，并在新增检查首次运行时抓到重试入口参数回归；修复后全量检查恢复为 103 项通过；
- 自定义 API 地址会去除末尾斜杠，避免健康检查拼出双斜杠。
- 贪吃蛇在超过 250 ms 的恢复帧只前进一步，不再回放多步不可见移动；昵称长度也已与服务端统一为 32。

### 最终结果

- F01–F13 及模块、架构、P0–P3、测试和性能建议的逐项结论已写入
  `classic_games_second_code_review_response_zh.md`；
- 最终全量检查为 103 项通过、0 失败；compileall、ruff、shell 语法和 diff whitespace 检查通过；
- 自定义 5010 端口派生检查通过；默认成绩库哈希、大小和修改时间保持不变；
- 修改仅保留在本地工作区，未提交或推送。
