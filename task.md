# 第三次审查修复记录

## 当前状态

- [x] 确认当前分支、远端和用户已有文档替换；
- [x] 完整阅读第三次审查任务书；
- [x] 建立 CG3-F01 至 F13 的可执行复现与结论；
- [x] 实现默认进程内本地存储和 Flask 可选适配器；
- [x] 修复 2048 输入及保存边界；
- [x] 修复可选 HTTP 客户端和 API 幂等边界；
- [x] 完成第一轮独立复查；
- [x] 完成第二轮独立复查；
- [x] 汇总逐项答复和验证证据。

## 初步核对

- F03 与 F04 已由审查给出的最小场景和当前控制流确认；
- F01、F02、F06 至 F11 的核心判断成立，共同反映保存所有权和数据语义不统一；
- F12 的 import 写磁盘成立，可由 app factory 直接消除；
- F13 不是孤立 Bug，但默认进程内 SQLite 与本地单机定位一致，本轮纳入架构修复；
- “95 与 103 不一致”的结论混淆了编号章节数和实际 PASS 数，需用 runner 输出反证；
- P2/P3 中的玩法、音频、手柄、编辑器、打包等是候选路线，不自动成为本轮缺陷。

## 验证记录

### 初始实现

- 新增共享游戏注册表、`LocalGameStore`、attempts/schema version 和本地适配器；
- 默认 `run.sh` 与独立游戏不再启动或依赖 Flask，Flask 改为显式 app factory；
- 同步本地事务前写原子 outbox，失败可跨进程恢复；人工重试复用 request ID；
- 幂等记录保存完整 payload hash 和响应快照，attempt 不再因个人最佳聚合被删除；
- 2048 `_move()` 返回实际移动结果，无效果命令连续消费，win/pause/reset 清理队列；
- 可选 HTTP 客户端的完成归档纳入 drain，永久 4xx 不进入重试队列，调度失败保留记录；
- 新增并发同 ID、低分 no-op、旧库导入、无 Flask 保存和退出确认检查。

### 第一轮独立复查

- 原套件基线实际为 95 个编号场景、103 个 PASS；网页审查把两者混为同一计数；
- 改造后全量检查为 107 个 PASS、0 失败；
- dummy 环境启动器在没有 Flask 的情况下完成启动和退出；
- 300 次含 outbox 的同步本地保存：median 1.020 ms、p95 1.337 ms、p99 1.498 ms、最大 1.774 ms；
- 五款游戏累计 20,000 次固定 seed 随机步进通过状态不变量；
- 复查发现数据库与 outbox 同时不可写时仍可一次 Esc 离开，已增加二次确认；持久 outbox 已写入时可安全返回并在下次启动恢复；
- 修复旧库缺少 `updated_at` 时的只读导入兼容性，以及 2048 低分响应降低客户端已确认分数的问题。

### 第二轮独立复查

- 100 轮本地客户端创建、读取和关闭后线程集合不变，文件描述符为 4→4；
- 16 worker 完成 240 次并发本地写入，attempt 数准确且 `PRAGMA integrity_check=ok`；
- 80 帧 dummy 渲染 p95：Tetris 2.399 ms、Snake 2.243 ms、2048 1.491 ms、Sokoban 1.109 ms、Zuma 4.337 ms；
- 损坏数据库会保留时间戳备份并重建；不可用路径降级为可游玩、保存失败可见；
- schema 升级前自动备份，旧 `scores` 同库或外部旧库均幂等导入；高于当前版本的库拒绝覆写；
- 检查并修正文档和测试中“replace 会删除旧记录”等失效语义；
- 第二轮修复后全量检查仍为 107 个 PASS、0 失败。

### 最终验证

- `./run_tests.sh`：107 个功能 PASS、0 失败，并继续完成固定 seed 压力套件；
- 最终压力结果：20,000 个玩法步进、五游戏 80 帧渲染、300 次本地保存、100 次资源循环和 240 次并发写入全部通过；
- 最终渲染 p95 为 1.087–4.604 ms，本地保存 p95 1.336 ms、p99 1.572 ms，FD 19→19，SQLite integrity check 为 ok；
- compileall、ruff、四个 shell 脚本语法和 diff whitespace 检查通过；
- 仓库旧 `data/scores.db` 的 SHA-256、大小和修改时间保持不变；
- CG3-F01 至 F13、模块、P0–P3、测试和性能建议的逐项结论见
  `classic_games_third_code_review_response_zh.md`。GitHub 交付状态以实际远端提交为准，不在提交前写入工作记录。
