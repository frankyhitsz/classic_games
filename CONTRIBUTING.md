# 参与开发

项目要求 Python 3.11。安装开发依赖后，从仓库根目录执行：

```bash
pip install -e '.[dev]'
./run_tests.sh
```

提交前至少运行 Ruff 和受影响的测试。涉及存储格式时，应同时覆盖迁移、损坏输入、
幂等重放和磁盘故障；涉及游戏规则时，应固定随机种子并说明规则版本是否变化。

Bug 报告请包含操作系统、Python/pygame 版本、复现步骤和终端错误。不要附带真实的
`games.db`；如需说明数据问题，请使用最小化的临时数据库或脱敏后的结构信息。
