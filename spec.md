# 第十八次审查修复规格

## 目标与范围

以 `984cd84` 为基线，核对第十八次审查的 30 条意见及配套任务。先修复会造成进度回退、
误删存档、恢复错误和备份不可读的问题，再处理协议兼容、推箱子会话和调试适配器生命周期。
已有实现若足以反驳意见，保留代码与测试证据，不重复改动。

## 设计约束

- 进度的 component 身份比较采用同一种 hash 定义；较旧 merge 不跨越 replacement set。
- 恢复标记先按文件名取锁，锁内读取和校验；锁等待计入恢复预算。
- 隔离存档必须比较加载时的身份；删除留下持久顺序屏障，旧 pending 不能复活。
- 归档发布前经同版本 reader 与语义检查；保留旧归档 reader；失败不留下冒充完整输出的文件。
- 已提交和已回滚的业务结果不因清理失败而改变；清理遗留可重试、可查看。
- 文件摘要和快照在 Windows 也按原始二进制读取；安全检查使用实际 lstat/fstat，不能把目录缓存缺失的链接数当作文件身份。
- 默认本机、离线、无遥测；不擅自决定代码许可、素材权利或发行签名身份。
- 不为达到覆盖率数字删减测试范围，不把未运行的平台验证写为通过。

## 验收

- 对 F01–F30 提供成立性、处理结果和证据；优化任务明确已完成与剩余边界。
- 新问题有可复现的定向测试；执行已有 storage、gameplay、stress 和 release 检查。
- 初版后至少两轮独立复查，修复发现的问题并重新验证。
- 只提交本次任务变更，推送到确认的远端分支，并检查 GitHub CI。
