# 第十三次审查修复规格

## 目标

逐条核对第十三次审查的 29 条发现与 122 项建议。优先处理启动恢复、归档边界、导入事务完整性、
2048 存档所有权和关闭释放等可能造成数据丢失、旧状态复活或并发覆盖的问题；不成立的判断保留
可复核的代码或运行证据。

## 本轮范围

- 普通客户端与可选 HTTP 服务在打开数据库前恢复未完成导入，并对维护占用或损坏事务给出可操作提示；
- 导出保护数据库、锁、活动 pending、旧版 pending 和恢复事务，且无 `--force` 时不会在竞争窗口覆盖文件；
- recovery evidence 扫描不跟随符号链接，归档路径统一为 POSIX 格式，完整备份准确反映遗漏项；
- import transaction 校验目标白名单、重复目标、阶段文件和回滚镜像内容，损坏时停止而不是继续发布；
- archive manifest 校验内部计数、完整性关系、版本、规则集和 evidence 元数据，并兼容读取旧版归档；
- replace restore 清理归档之外的旧表、旧版 pending 和活动恢复工件，避免旧状态在之后重新出现；
- state outbox 对同一 `(revision, op_id)` 的不同 payload 报冲突，拒绝回滚 marker 原子发布且可检测损坏；
- 2048 在所有权 CAS 确认前禁止输入，关闭时把 release 意图交给后端，即使前一个保存仍在执行；
- 数据 CLI 提供未完成事务的查看和显式恢复入口，并补充必要的库存信息。

## 约束

- 维护锁与 application lease 是协议边界；不在锁外打开待恢复数据库，也不让导出目标覆盖协议控制文件；
- 所有写入数据库目录的恢复路径必须先做词法 containment 和逐级 no-symlink 检查，再允许解析；
- 新事务使用带内容摘要的格式；仍能识别已有旧事务，但不会把无法验证的新事务当成安全数据；
- 完整归档不能静默省略持久化工件。部分归档可记录 omission，但不得读取数据目录外的内容；
- `restore-replace` 保留操作前回滚镜像，但恢复结果只包含当前产品 schema 与归档内容；
- GitHub branch protection 和许可证选择取决于仓库权限及权利人决定，不用文档声明冒充完成。

## 验收标准

- F01–F29 和 P0–P3 共 122 项均有成立性、处理状态和证据；
- 启动恢复、锁竞争、符号链接、导出发布竞争、事务内容篡改、重复目标和跨平台路径有自动化测试；
- 完整导出包含所有活动协议工件或明确失败，replace restore 后旧工件和未知表不会复活；
- 2048 claim ACK 前不接受移动，关闭阶段 release 不会滞留在游戏对象的 dirty buffer；
- 完成两轮独立复查，运行 lint、compile、storage、gameplay、stress、package/release 验证；
- 本次变更单独提交并推送到已确认远端，随后核对远端提交和 CI 状态。
