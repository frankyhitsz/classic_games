# 二次审查修复规格

## 目标

核实 `classic_games_second_code_review_local_first_taskbook_zh.md` 对当前
`main` 分支的判断，修复能够由代码路径或实际运行复现的问题，并明确区分当前缺陷、
可选体验功能和需要单独设计的数据架构改造。

## 本轮范围

- 成绩异步保存的确认、失败、重试、榜单刷新和退出收尾；
- 自定义后端端口和测试数据库隔离；
- 俄罗斯方块同义物理按键及大时间步跨方块问题；
- HTTP 客户端资源关闭、结果契约和并发退避状态；
- 分数更新单调性、数据库异常的 JSON 响应和统计字段语义；
- 审查中能低风险验证的玩法及脚本边界；
- 对上述行为增加隔离检查，并完成两轮独立复查。

## 非目标

- 本轮不引入账号、公网排行、反作弊、赛季或遥测；
- 不把 Flask/SQLite 一次性重写成 profiles、attempts、save slots 全套数据平台；
- 不把 7-bag、hold、ghost、撤销、音频、手柄、IME、关卡编辑器等产品功能冒充 Bug 修复；
- 不在缺少迁移和兼容方案时改变现有玩家数据库的位置或格式；
- 未获得本轮 GitHub 提交授权，不提交或推送。

## 关键决策

1. 保留当前可选离线运行和 Flask 记分结构，先补齐现有异步保存闭环。
2. 保存成功必须收到结构合法的 `ok=true` 和正整数记录 ID；失败必须可见并可重试。
   同一次逻辑保存的自动重试复用请求号，服务端不得生成重复成绩。
3. 结算榜单应在保存确认后读取，旧 Future 不得覆盖新结算状态。
4. 共享 `BackendClient` 在启动器退出时统一 drain/close，子游戏不单独关闭它。
5. Tetris 按实际物理键记账，并在方块代际变化后停止本帧补步。
6. 测试必须显式继承临时数据库路径，不能创建或修改仓库默认成绩库。

## 验收标准

- CG2-F01 至 F13 均有“确认、部分成立或反驳”结论及证据；
- 保存中、保存成功、保存失败三种状态可区分，失败可由结算页重试；
- POST 确认前不读取结算榜单，确认后能刷新；
- 返回菜单或正常关闭时，共享客户端会等待已开始的保存任务收尾；
- 自定义端口的服务端、健康检查和客户端地址一致；
- 完整测试不接触默认 `data/scores.db`；
- Tetris 同义键释放和软降/重力跨代场景有独立检查；
- 数据库错误统一返回 JSON，2048 同一记录更新不得降低分数；
- 项目原有检查、静态检查、随机压力和两轮复查全部通过。
