# 二次代码审查核对结论

本文记录对 `classic_games_second_code_review_local_first_taskbook_zh.md` 的本机核对结果。
判断依据是当前代码、可执行复现、隔离测试和实测数据，不把建议的产品路线当成既有需求。

结论分为三类：

- **已修复**：问题可复现，已修改并加入检查；
- **部分采纳**：问题中的事实成立，但建议的整体重构超出了修复所需范围；
- **不作为缺陷**：属于可选功能、架构偏好或缺少实测证据的优化，不据此改动当前行为。

## CG2-F01 至 F13

| 编号 | 结论 | 处理 |
| --- | --- | --- |
| F01 | 已修复 | Future 现在会被轮询并严格解析；结算层显示保存中、已保存或失败，失败可按 S 重试。共享客户端在启动器退出时等待已开始的任务。 |
| F02 | 已修复 | 排行榜只在保存 ACK 后刷新；保存和排行榜都有 generation，旧 Future 不会覆盖新一局。 |
| F03 | 已修复 | 推箱子的累计分改为 pending/confirmed，只有合法 ACK 才更新 confirmed。 |
| F04 | 已修复 | 2048 使用有限自动重试、严格响应解析、两项输入队列和最终分排队；重置不会丢掉已排队的最终分。每次逻辑保存复用请求号，响应丢失后重试不会重复记分。 |
| F05 | 已修复 | `run.sh` 从 `GAMES_PORT` 派生并导出 `GAMES_API_URL`，监听地址和连接地址分开，健康检查使用同一地址。 |
| F06 | 已修复 | 直接导入测试也使用临时 `GAMES_DB`；脚本比较默认数据库及其 sidecar 的前后哈希，发现污染立即失败。app factory 是另一项结构改造，不是实现隔离的必要条件。 |
| F07 | 已修复 | Tetris 记录物理键集合，方向键与 WASD 的同义键可独立按下和释放，并保留最近按下的水平方向。 |
| F08 | 已修复 | Tetris 增加 piece generation 和帧时间上限；锁定后本帧剩余软降/重力不作用于新方块，暂停和失焦会清累计状态。 |
| F09 | 已修复 | BackendClient 有幂等 `close()` 和 `drain()`，会关闭线程池与各线程 Session；独立游戏关闭自有客户端，启动器统一关闭共享客户端。 |
| F10 | 部分采纳 | 无锁共享退避和“旧成功清除新失败”确实成立，现已加锁、分离读写退避并比较请求开始时间。为单一本机服务按每条 GET 路径继续拆分退避没有实际收益，可靠写入会主动绕过读退避。 |
| F11 | 已修复 | 使用 `submission_id` 更新时保存 `MAX(existing, incoming)`；低分不会覆盖已有 2048 高分，也不会用低分的 extra 覆盖最终状态。 |
| F12 | 部分采纳 | 原 `plays` 实际是保留的成绩行数，已改名为 `records`。真正的 attempt、完成局数和启动次数需要新的数据模型，当前数据库没有这些事实，不能通过改查询凭空得到。 |
| F13 | 已修复 | `sqlite3.Error` 统一记录 traceback 并返回 JSON 503；分数、排行、统计和最近记录的错误路径都已覆盖。 |

## 其他模块建议

### 启动器

1. 元数据多处重复：事实成立，属于注册表重构；五个固定游戏下没有造成功能错误，留作新增游戏时处理。
2. 五卡固定布局：当前产品正好是五款游戏，不把尚未提出的动态增删能力当成缺陷。
3. 完整键盘和手柄导航：是可访问性功能，当前没有这项需求，未改。
4. IME：`KEYDOWN.unicode` 对组合输入支持有限，判断成立；实现完整输入控件需要明确交互规格，本轮不做半套 IME。
5. 名字长度契约：已把客户端上限从 16 调整为与服务端一致的 32。
6. 启动失败反馈：原判断成立；现会在启动器内显示失败提示，玩家可再次点击，终端保留异常详情。专用崩溃页是后续功能。
7. 描述换行注释：代码并未换行，已把误导注释改成短单行描述，不为当前短文案增加无用布局逻辑。
8. 模块文档：已改为五款本地安装游戏，不再声称列表来自后端。
9. 在线/离线措辞：已改为“成绩服务可用/不可用”，离线游玩本身不被描述为异常。
10. hover 插值：当前代码已使用 `frame_dt`，该条是审查时遗漏现状，不需要再修。

### 共享 UI

1. 魔法字符串和 2. 状态动作散落：是维护性问题，当前转换路径已有覆盖；完整状态机重构不应夹带在可靠性补丁中。
3. 每帧临时 Surface 和 4. 每帧 Button：是可能的优化点，但本机 dummy 渲染 p95 仅 2.25–4.43 ms，没有当前帧预算问题。
5. 可缩放高 DPI、10. 设置/音频/主题/映射/辅助层：均是新功能，不是当前窗口模式的错误。
6. CJK fallback：当前已有 PingFang、Hiragino、微软雅黑等系统字体链；“只使用任意系统字体”的描述不准确。正式打包若要求所有系统零依赖显示中文，再决定是否随包分发字体。
7. broad exception：离线读取边界有意捕获异常，防止网络故障退出游戏；保存路径现在会把失败变成可见状态，不再静默吞掉。
8. 保存状态：已统一补齐。
9. 崩溃页：启动失败已有 UI 提示；完整崩溃页和轮转日志属于发行功能。

### 俄罗斯方块

1–3 均已证实并修复：物理同义键、跨代 catch-up 和显式 falsy board 都有独立检查。
4–6 的 7-bag、可注入 RNG、lock delay、ghost、hold 是规则或舒适性选择，审查也承认不是竞技规则必选项；未改变当前玩法。
7 的背景缓存没有性能证据；8 的帮助页属于内容设计，README 已记录现有操作和等级规则。

### 贪吃蛇

1–2 的长帧问题成立：超过 250 ms 的恢复帧现在最多移动一格并丢弃不可见积压，普通低帧率仍可有限补步。
3 的底纹缓存没有实测必要性。
4 的最高分和玩法模式、5 的可注入 RNG/replay、6 的食物形状是新功能；色弱辅助值得在统一可访问性设计中处理，不单独临时增加符号。

### 2048

1 已改为长度 2 的有序输入队列；2 已删除无用途的 `_pending_state`。
3 的重置确认、4 的本地最高分、5 的撤销、6 的存档继续、7 的 RNG 注入和 8 的逻辑拆分是产品或结构设计，需要单独确定规则。
9 的静态背景缓存没有当前性能证据。

### 推箱子

1 已由 ACK 驱动；2 已改正注释；4 已拒绝零箱子/目标关卡。
3 的通用关卡封闭性、可达区域和死格验证对关卡编辑器有价值；当前内置 16 关已经求解器验证，玩家和箱子也始终位于 floors，不能据此认定现有关卡有错。
5–6 是计分模型改版；7–10 是死锁、提示、选关和编辑器功能；11 是窗口体验选择；12 在任务书中也确认当前规模可接受。均未作为 Bug 修改。

### 祖玛

1 已有多个独立/重叠反应、递归合拢和顺序单调检查。
2 的 `pop(0)` 只作用于上限约 48 的入口队列，当前复杂度可忽略。
3 已在过关时清空飞行弹丸，避免结算层冻结；4 已把视觉脉冲改为游戏时间，暂停不再推进。
5 的临时 Surface 没有帧预算证据；6–9 是记录模型、训练、辅助符号、RNG、道具和编辑器功能，不属于现有祖玛规则缺陷。

## “本地优先”架构建议

任务书把“默认无 HTTP、Flask 仅作可选适配器”作为既定目标，但当前项目说明明确包含 Flask 与 SQLite 排行服务，用户没有提出移除它。游戏在服务不可用时本来就能继续游玩；本轮已让失败保存可见和可重试。

`LocalGameStore`、操作系统用户目录、schema migration、attempts/progress/save/settings
是一条可行的桌面发行路线，但会改变数据位置、接口、统计语义和迁移责任。缺少产品决策和旧库迁移方案时直接实施，风险高于保留现有架构。因此第 5 节作为后续方案保留，不作为本轮缺陷修复。

## P0 任务清单

| 任务 | 结论 |
| --- | --- |
| P0-01、02 | 已完成数据库隔离和端口传播。 |
| P0-03、04 | 已完成保存状态、ACK 后刷新和 generation 防旧结果覆盖；无需为此额外引入一个命名为 `ScoreSaveController` 的类。 |
| P0-05 | 已开始的保存由共享客户端收尾；返回菜单后失败仍可见并可重试。 |
| P0-06、07 | 已完成推箱子与 2048 生命周期修复；2048 重试带幂等请求号。 |
| P0-08、09 | 已完成 Tetris 物理键和 piece generation 修复。 |
| P0-10、11 | 已完成 JSON 数据库错误和严格客户端 ACK 契约。 |
| P0-12 | F01–F13 的核心路径已有隔离检查；“CI 阻止合并”依赖仓库工作流授权，不是本地修复条件。 |

任务书列出的 12 个 P0 测试场景均有对应覆盖：默认库指纹、自定义地址、可见失败、ACK 排行顺序、关闭收尾、推箱子确认、2048 自动重试与重置、Tetris 同义键与跨代、畸形响应。

## P1、P2、P3 任务清单

### P1

- P1-01 至 P1-08 都依赖尚未确认的“改为进程内本地仓储”产品决策，本轮不采纳。
- P1-09 至 P1-11 是注册表、启动器拆分和状态枚举重构；代码规模和现有测试没有显示必须立即实施。
- P1-12 的资源关闭已经完成。
- P1-13 的 app factory 有利于大型 Flask 项目，但当前测试已经通过环境注入隔离，import 初始化仍是受支持入口的现有契约。
- P1-14 至 P1-16 的 pytest、CI、打包元数据和依赖锁定是工程路线，不是已复现 Bug；现有独立子进程检查能可靠运行。
- P1-17 已部分落实为启动器错误提示和服务端 traceback；完整崩溃页取决于日志/打包设计。
- P1-18 中与本轮行为相关的 README、规格和验证记录已同步；备份和迁移说明要等数据架构确定。

### P2

- P2-01 仅有 Tetris 同义键问题是现存缺陷，已修；统一全游戏 InputManager 是重构。
- P2-02 至 P2-08 是 IME、设置、音频、缩放、字体资产、手柄和可访问性新功能，均需产品规格。
- P2-09、P2-10 不能仅凭“每帧创建对象”认定需要优化；当前渲染实测没有超预算，祖玛 p95 为 4.43 ms。
- P2-11 中祖玛墙钟已改为游戏时间；全游戏可注入 RNG/Clock 是可重放架构，不是当前规则错误。
- P2-12、P2-13 是纯逻辑拆分和存档功能，未采纳。
- P2-14 已做本机基准，但未把单台 dummy 结果冒充跨平台性能承诺。
- P2-15 做了 40 轮客户端资源循环和线程基线检查；30–60 分钟 soak 与 100 次完整 GUI 切换仍是发行前建议。
- P2-16 是数据导入导出功能，需先确定数据架构。

### P3

P3-01 至 P3-16 全部属于玩法扩展、编辑器、档案、挑战、新游戏、本地化、打包、许可和社区材料。它们多数是合理的候选路线，但没有证据表明当前实现违反既有需求，不能标成当前缺陷。正式发行前应单独处理 P3-14、P3-15；增加新游戏前应先确定注册、记录和测试契约。

## 测试、性能和质量门槛

- 第 7 节中与现有规则有关的输入、保存、2048 合并、推箱子完整闯关和祖玛反应场景已有回归覆盖；迁移、设置损坏、存档、编辑器和打包测试依赖尚不存在的功能。
- pytest、属性测试、mutation testing、JUnit 和覆盖率是工具选择，不能用是否采用某一框架代替当前测试是否隔离、可复现和能抓到回归。
- 五款游戏各渲染 80 帧的 p95 为 2.25–4.43 ms；18,000 次随机输入/时间步进没有破坏状态不变量；40 轮 BackendClient 生命周期后线程回到基线；120 次并发 SQLite 写入完成。
- 第 8 节的 60 FPS、可见保存状态、退出收尾和 Tetris 长帧要求已满足或已有检查。强制缓存所有静态 Surface、100 次 GUI 切换、30–60 分钟 soak 和 500 ms 磁盘模拟是发行基准计划，不是当前运行结果。
- 第 9 节把默认无 HTTP、用户目录、schema migration、pytest/CI 和任意覆盖率阈值列为“最低要求”，这依赖任务书自行设定的发行架构，不能反推为当前项目的验收条件。测试隔离、保存可靠性、准确字段语义、静态检查和文档一致性已落实。

## 最终验证

- `./run_tests.sh`：103 项通过，0 失败；
- `ruff check client server tests`：通过；
- `python -m compileall -q client server tests`：通过；
- `bash -n run.sh run_tests.sh`：通过；
- `git diff --check`：通过；
- 默认 `data/scores.db` 及 sidecar 的测试前后指纹一致。

本轮修改只保留在本地工作区，没有提交或推送。
