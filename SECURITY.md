# 安全说明

默认桌面模式只访问本机文件，不启动网络服务。可选 Flask 适配器没有身份验证，默认只监听
`127.0.0.1`，不应暴露到公网。

发现可能导致任意文件读写、代码执行、数据静默丢失或绕过本机 API 边界的问题时，请通过
GitHub 仓库的私密漏洞报告功能联系维护者，不要先公开可直接利用的样例。报告中请说明受影响
版本、平台、复现条件和最小修复建议；不要提交包含个人成绩或用户目录信息的数据库文件。
